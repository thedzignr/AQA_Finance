/** Distinct hues for charts and work streams — not a single-colour scale. */
export const CHART_COLORS = [
  "hsl(var(--chart-1))",
  "hsl(var(--chart-2))",
  "hsl(var(--chart-3))",
  "hsl(var(--chart-4))",
  "hsl(var(--chart-5))",
] as const;

export const INCOME_FILL = "hsl(var(--chart-1))";
export const EXPENSE_FILL = "hsl(var(--chart-3))";
